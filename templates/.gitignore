# Build output (regenerated by tools/build.*)
/site/

# App generated data (regenerated by tools/engine/build_data.py)
/app/public/data/

# Node / Vite (builds happen on C:\, never on the Drive mount)
node_modules/
dist/
.vite/

# Wrangler / Cloudflare (local deploy state)
.wrangler/

# Local env
.env
secrets.env
*.local

# enrichment orchestration artifacts (regenerable)
tools/enrichment/assignments/
